请在 /home/ubuntu/data/PSAgent 里，取消最近对 `ps_favored_trap` controlled sim landscape 的那轮 tweak，回到**上一个版本的 sim 实验环境**，然后对所有带超参数的算法做统一超参 sweep。最后对 13 个算法分别取“各自最优超参”的结果，汇总成一张总表，保留原来已有的所有指标。

先阅读并核对这些文件：
- scripts/run_barriershare_controlled_sim.py
- outputs/barriershare_controlled_sim_prefix_dedup_ps_favored_trap_v9_eta02_eps002_switch_8_plus3_v1/run_config.json
- outputs/barriershare_controlled_sim_prefix_dedup_ps_favored_trap_v9_eta02_eps002_switch_8_plus3_v1/ps_favored_trap_compare.md
- outputs/barriershare_controlled_sim_prefix_dedup_ps_favored_trap_v9_eta02_eps002_switch_8_plus3_v2/run_config.json
- outputs/barriershare_controlled_sim_prefix_dedup_ps_favored_trap_v9_eta02_eps002_switch_8_plus3_v2/ps_favored_trap_compare.md
- baselines/risky_ps.py
- baselines/risky_ps_linear.py
- baselines/direct_multistage_exp3.py
- baselines/direct_multistage_exp3_local.py
- baselines/epsilon_exp3.py
- baselines/naive_mixed.py
- baselines/naive_mixed_avg.py
- baselines/random_path.py

目标：
1. 取消 v2 那轮 landscape tweak，恢复到 **v1 那套 sim 环境**
2. 在这套恢复后的 controlled sim 环境里，对 13 个算法做超参 sweep
3. 对每个算法选出“本算法最优超参”
4. 用每个算法的最优结果生成一张总对比表
5. 保留旧图那套所有指标字段
6. trap 修改时间统一设为 `T/8`

一、必须恢复到的实验环境
--------------------------------

请明确恢复到 v1 的 controlled sim 环境，也就是：
- 不使用 v2 那轮对 `_ps_favored_selected_good_leaf_paths()` 和 `_ps_favored_trap_base_probability()` 的 tweak
- 恢复到 `outputs/barriershare_controlled_sim_prefix_dedup_ps_favored_trap_v9_eta02_eps002_switch_8_plus3_v1` 对应的 landscape 逻辑

要求：
- 如果当前工作树里已经是 v2 逻辑，请只修改 `scripts/run_barriershare_controlled_sim.py`
- 恢复后要让：
  - selected-good 逻辑回到 v1
  - exact-best / oracle 逻辑回到 v1
  - target_good 的成本逻辑回到 v1
- 不要改 baseline 算法本体
- 不要改 metric 逻辑
- 不要覆盖已有 v1 / v2 输出目录

二、统一实验口径
--------------------------------

请严格使用旧图对应的 controlled sim 代码和指标口径：

- runner:
  - `scripts/run_barriershare_controlled_sim.py`
- tree spec:
  - `analysis/tree_specs/shared_basin_strong_4of5_prefix_dedup.json`
- `tree_spec_role_mode = spec_or_agent_id`
- `tree_spec_cost_mode = ps_favored_trap`
- `trap_switch_denominator = 8`
- `horizon = 1000`
- `seeds = 0 1 2 3 4`
- `cost_noise = 0.02`
- `specialist_fraction = 0.15`

说明：
- `trap_switch_denominator = 8` 就是你要的 `T/8`
- 不要改成别的 switch 时间
- 不要用 repeated smoke runner
- 不要用 LLM
- 不要 orchestrate

三、要跑的 13 个算法
--------------------------------

请统一比较这 13 个方法：

1. `risky_ps_old`
2. `risky_ps`
3. `risky_ps_ix`
4. `risky_ps_safe_conditional`
5. `risky_ps_safe_conditional_ix`
6. `risky_ps_direct_cost`
7. `direct_multistage_exp3`
8. `epsilon_exp3`
9. `naive_mixed`
10. `random_path`
11. `naive_mixed_avg`
12. `risky_ps_linear`
13. `direct_multistage_exp3_local`

如果当前 runner 已经接入这些方法，就直接复用。
不要改旧方法行为。

四、超参 sweep 要求
--------------------------------

请只对“有超参数设定”的算法做 sweep。

至少覆盖这些超参轴：

### 1. eta 类方法
对所有吃 `eta` 的方法做 sweep。
建议至少 sweep：
- `0.02`
- `0.05`
- `0.1`
- `0.2`
- `0.5`

适用方法至少包括：
- `risky_ps_old`
- `risky_ps`
- `risky_ps_ix`
- `risky_ps_safe_conditional`
- `risky_ps_safe_conditional_ix`
- `risky_ps_direct_cost`
- `risky_ps_linear`
- `direct_multistage_exp3`
- `direct_multistage_exp3_local`
- `epsilon_exp3`

### 2. epsilon 类方法
对所有吃 `epsilon` 的方法做 sweep。
建议至少 sweep：
- `0.0`
- `0.01`
- `0.02`
- `0.05`
- `0.1`

适用方法至少包括：
- `risky_ps_old`
- `risky_ps`
- `risky_ps_ix`
- `risky_ps_safe_conditional`
- `risky_ps_safe_conditional_ix`
- `risky_ps_direct_cost`
- `risky_ps_linear`
- `epsilon_exp3`

### 3. 无超参数方法
这些方法不做 sweep，只跑一次：
- `naive_mixed`
- `naive_mixed_avg`
- `random_path`

如果你发现某个方法还有额外合法超参轴，而且当前 runner 已经支持，可以补充；但不要擅自改算法定义。

五、最优参数的选取规则
--------------------------------

请在最终汇总前，先对每个方法内部选“最优超参”。

要求：
1. 明确指定主排序指标
建议主指标：
- `regret/T` 最小

2. 若主指标并列，再按：
- `overall avg cost` 最小
- `post-switch avg regret` 最小

3. 每个方法都要输出：
- 本方法最优超参组合
- 本方法最优结果的完整指标

六、输出要求
--------------------------------

请输出到新的目录，不要覆盖旧结果。
建议目录例如：

- `outputs/barriershare_controlled_sim_prefix_dedup_ps_favored_trap_v9_sweep_bestof_v1`

并保留中间 sweep 结果与最终 best-of 汇总。

建议至少生成：

### sweep 级输出
- `sweep_runs_manifest.json`
- `sweep_results_long.json`
- `sweep_results_long.csv`

### best-of 汇总输出
- `best_params_by_method.json`
- `best_params_by_method.csv`
- `best_of_compare.json`
- `best_of_compare.csv`
- `best_of_compare.md`

如果方便，也请保留每个 sweep run 对应的原始 controlled sim 标准产物。

七、最终总表必须保留旧图那套指标
--------------------------------

最终 `best_of_compare` 总表至少要保留这些列：

- `method`
- `best_eta`（如适用）
- `best_epsilon`（如适用）
- `regret/T`
- `overall avg cost`
- `tail20 avg cost`
- `post-switch avg regret`
- `target good frac`
- `trap frac`
- `shared path frac`

如果原始字段名是：
- `regret_per_t_mean`
- `average_cost_mean`
- `tail20_avg_total_cost_mean`
- `post_switch_avg_regret_mean`
- `target_good_fraction_mean`
- `trap_basin_fraction_mean`
- `shared_path_fraction_mean`

请在最终表里保留用户友好的展示名，但不要改原始 summary 逻辑。

八、验证要求
--------------------------------

请完成这些验证：

1. 静态检查
```bash
python -m py_compile scripts/run_barriershare_controlled_sim.py
```

2. 恢复检查
请明确确认当前代码已经恢复到 v1 landscape，而不是 v2。

3. 实际运行
请完成所有需要的 sweep run，并生成 best-of 汇总。

4. 合法性检查
请确认：
- 每个有超参的方法都确实扫到了对应参数网格
- 每个方法都成功选出了 best params
- 最终总表覆盖 13 个方法

九、最终回复必须包含
--------------------------------

1. 修改了哪些文件
2. 是否已恢复到 v1 sim 环境
3. 恢复时改回了哪些关键逻辑
4. 实际 sweep 了哪些超参网格
5. 哪些方法不需要 sweep
6. 输出目录路径
7. `best_params_by_method` 文件路径
8. `best_of_compare` 文件路径
9. 13 个方法各自的最优参数
10. 最终总表
11. 哪个方法在 best-of 比较里最好
12. 明确说明：
- 没有改 baseline 算法
- 没有改 metric 逻辑
- 没有覆盖旧结果
- 没有跑 LLM
- 没有跑 orchestrate
- trap 修改时间统一使用 `T/8`

一句话目标：
回到 v1 的 `ps_favored_trap` controlled sim 环境，在 `T/8` switch 下对 13 个算法做统一超参 sweep，选出每个算法自己的最优参数，并用旧图那套全部指标生成最终 best-of 总对比表。





改完并重跑了同一组诊断。

修改的文件：
- [envs/executors/telecom_llm_bench_executor.py](/home/ubuntu/data/PSAgent/envs/executors/telecom_llm_bench_executor.py)

新增文件：
- 无
- [scripts/run_llm_path_sweep_diagnostic.py](/home/ubuntu/data/PSAgent/scripts/run_llm_path_sweep_diagnostic.py) 这次没改

**这次加硬的规则**

`stage3`：
- blocker family 的保留/排除改成硬门槛，不能只凭“没看到证据”就排除
- 对 `apn / roaming / permission / network` 明确要求：如果 task profile 下它们是高优先级，没有反证就不能排除
- `fast` 只能紧凑验证 top 1-2 family，不能在弱证据下同时确认多个 family
- `deep` 必须对 top family 做 decisive evidence cross-check 才能定 `observed_state`

对应代码：
- [telecom_llm_bench_executor.py:926](/home/ubuntu/data/PSAgent/envs/executors/telecom_llm_bench_executor.py:926)
- [telecom_llm_bench_executor.py:1253](/home/ubuntu/data/PSAgent/envs/executors/telecom_llm_bench_executor.py:1253)

`stage4`：
- repair 前置条件写成硬规则，不再允许“常见就修”
- 明确要求：
  - APN repair 要有 APN evidence
  - roaming repair 要有 roaming-side evidence
  - permission/network repair 要有对应 blocker evidence
- `fast` 不允许多步 repair chain，也不允许没 direct blocker evidence 就做 specialist repair
- `deep` 在 blocker family 仍模糊时，优先 narrower/deferred action，不允许用更多预算硬修过去

对应代码：
- [telecom_llm_bench_executor.py:945](/home/ubuntu/data/PSAgent/envs/executors/telecom_llm_bench_executor.py:945)
- [telecom_llm_bench_executor.py:1322](/home/ubuntu/data/PSAgent/envs/executors/telecom_llm_bench_executor.py:1322)

`stage5`：
- `transfer` 不能再当默认兜底，只能在“关键证据拿不到且本 stage 预算内补不齐”或“明确需要外部/人工处理”时用
- `repair_all` 不能在 family 还实质性模糊时作为 fallback
- `fast` 若没达到 minimum verification floor，必须转保守 terminal decision
- `deep` 在 decisive evidence 没核验前，不允许 `transfer` / `close`

对应代码：
- [telecom_llm_bench_executor.py:963](/home/ubuntu/data/PSAgent/envs/executors/telecom_llm_bench_executor.py:963)
- [telecom_llm_bench_executor.py:1443](/home/ubuntu/data/PSAgent/envs/executors/telecom_llm_bench_executor.py:1443)

**mismatch 规则的硬化**

我把 mismatch 从“预算提示”改成了“可否 finalize 某类决策”的硬约束：

- `deep-required + fast-agent`
  - 可以缩 scope
  - 但不能跳过 decisive verification 后仍输出高自信 blocker / repair / terminal 决策
  - 预算不够时必须更保守
- `fast-required + deep-agent`
  - 达到 minimum evidence floor 后必须停
  - depth 不是继续做低收益 verification 的理由

对应代码：
- [telecom_llm_bench_executor.py:903](/home/ubuntu/data/PSAgent/envs/executors/telecom_llm_bench_executor.py:903)
- [telecom_llm_bench_executor.py:1065](/home/ubuntu/data/PSAgent/envs/executors/telecom_llm_bench_executor.py:1065)
- [telecom_llm_bench_executor.py:1092](/home/ubuntu/data/PSAgent/envs/executors/telecom_llm_bench_executor.py:1092)

**prompt 文本层面的关键变化**

这次不是只加原则，而是把这些字段真正变成 binding text：
- `search_policy`
- `stop_policy`
- `capability_mismatch_policy`
- `stage_specific_hard_constraints`

并把 stage3/4/5 的 rulebook 同时塞进：
- system prompt
- user prompt JSON

所以模型现在不只是“知道预算不同”，而是明确读到：
- 什么时候不能排除 family
- 什么时候不能 repair
- 什么时候不能 transfer / repair_all

**静态检查**

执行：
```bash
python -m py_compile \
  envs/executors/telecom_llm_bench_executor.py \
  scripts/run_llm_path_sweep_diagnostic.py
```

结果：通过。

**诊断跑**

执行：
```bash
export PSAGENT_LLM_BENCH_MODEL=gpt-4o-mini
python scripts/run_llm_path_sweep_diagnostic.py \
  --data data/derived/telecom_mms_fixed_tree_base_v2_100_capabilities_time_profile_switch/tasks.json \
  --family-kind shared_basin_strong_prefix_dedup_profile_switch \
  --output-dir /tmp/llm_path_sweep_diagnostic_v2 \
  --seed 0 \
  --max-paths-per-task 5 \
  --path-mode representative
```

结果：通过。  
样本量仍是 `4 task × 5 path = 20`。

输出目录：
- v1: `/tmp/llm_path_sweep_diagnostic`
- v2: `/tmp/llm_path_sweep_diagnostic_v2`

**和旧版诊断相比**

`final_action` 分化：
- 没有增加，反而下降
- v1: `1/4` task 有 action diversity
- v2: `0/4`
- 典型退化是第二个 trap task：
  - v1: `repair_all / transfer` 分叉
  - v2: 全部收敛成 `transfer`

`terminal penalty spread`：
- 没有更稳定，整体更弱
- overall mean terminal spread:
  - v1: `2.875`
  - v2: `0.75`
- 有正 terminal spread 的 task 数：
  - v1: `3/4`
  - v2: `2/4`

细看：
- 第二个 trap task 从 `9.5` spread 直接塌成 `0.0`
- target task 倒是从 `1.0` 提到 `2.0`
- 但整体上 path differentiation 变弱了，不是变强

`offline path_match` 和 terminal 的关系：
- 数值上略有改善，但仍不稳定
- overall corr mean:
  - v1: `-0.385`
  - v2: `-0.134`
- v2 里 target task 出现了正相关 `0.369`
- 但 specialist task 仍是强负相关 `-0.637`
- trap task 之一直接塌成全同 terminal，相关性失去判别力

所以结论是：
- **prompt 约束确实更硬了**
- 但这版硬化把一部分 path 差异压成了“统一保守 transfer”
- 行为差异没有被稳定放大，反而出现了过度保守收缩

**我对这轮结果的判断**

当前问题已经不是“约束不够硬”，而是这版约束把 stage5 的 conservative gate 压得过重，导致：
- path 差异没有更多转化成 terminal decision 分层
- 相反，一部分 path 被统一推向 `transfer`

如果继续往前，我建议下一步不是再加硬，而是做一版更窄的修正：
- 保留 `stage3` 的 family elimination hard gate
- 保留 `stage4` 的 repair precondition gate
- 适当放松 `stage5 transfer` 约束，避免把“证据未满”直接统一压成 `transfer`

明确说明：
- 没有改 cost 聚合公式
- 没有改 evaluator
- 没有改 family / dataset
- 没有跑正式 repeated smoke
- 没有 orchestrate